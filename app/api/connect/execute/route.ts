import{NextResponse}from"next/server";import{executeProduction}from"@/lib/production-executor";export async function POST(r:Request){const b=await r.json().catch(()=>null);if(!b?.consent)return NextResponse.json({ok:false,code:"USER_CONSENT_REQUIRED"},{status:409});const x=await executeProduction({invocationId:b.invocationId,actionId:b.actionId,executor:b.target?.kind==="device"?"device":"provider",adapter:b.target?.adapter||"missing"});return NextResponse.json(x,{status:x.ok?200:409})}
