import{NextResponse}from"next/server";export const dynamic="force-dynamic";export async function GET(r:Request){const kind=new URL(r.url).searchParams.get("kind")||"custom";const candidates=[{id:"openai",name:"OpenAI",kind:"provider",adapter:"openai",available:!!process.env.OPENAI_API_KEY,reason:"OPENAI_API_KEY_REQUIRED"},{id:"gemini",name:"Gemini",kind:"provider",adapter:"gemini",available:!!process.env.GEMINI_API_KEY,reason:"GEMINI_API_KEY_REQUIRED"}].filter(()=>kind==="ai"||kind==="custom");return NextResponse.json({ok:true,kind,candidates,discovery:{wifi:"native-lan-agent-required",bluetooth:"user-gesture-required",qrFallback:true}})}
