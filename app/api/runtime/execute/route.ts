import{NextResponse}from"next/server";import{executeProduction}from"@/lib/production-executor";import{simulate}from"@/lib/runtime-simulator";export async function POST(r:Request){const b=await r.json().catch(()=>null);if(!b?.invocationId||!b.actionId||!b.adapter)return NextResponse.json({ok:false,code:"INVALID_COMMAND"},{status:400});const x=process.env.LONG_RUNTIME_MODE==="simulator"?simulate(b):await executeProduction(b);return NextResponse.json(x,{status:x.ok?200:409})}
