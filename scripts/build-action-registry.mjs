import fs from"node:fs";const c=JSON.parse(fs.readFileSync("reports/action-coverage.json","utf8")),contracts=c.controls.map(x=>{const actionId=x.literalActionId||`dynamic.${c.surface}.${x.file.replace(/[^a-z0-9]+/gi,".").toLowerCase()}.${x.line}.${x.column}`;return{actionId,actionIdPattern:x.literalActionId?null:x.explicitActionId,name:`Button at ${x.file}:${x.line}`,surface:c.surface,screen:x.file,ownerCore:"eventspace",status:"gated",preconditions:["executor-and-receipt-mapping-required"],executor:"job",receiptSchema:"unconfigured",telemetry:"action-ledger/v2"}});if(new Set(contracts.map(x=>x.actionId)).size!==contracts.length)throw new Error("Duplicate actionId");fs.writeFileSync("reports/action-registry.json",JSON.stringify({generatedAt:new Date().toISOString(),surface:c.surface,contracts},null,2)+"\n");console.log(`ACTION REGISTRY PASS: ${contracts.length} unique source contracts.`)
