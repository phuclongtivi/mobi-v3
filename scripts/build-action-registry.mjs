import fs from "node:fs";
const coverage=JSON.parse(fs.readFileSync("reports/action-coverage.json","utf8"));const contracts=coverage.controls.map(control=>{const match=control.explicitActionId?.match(/"([^"]+)"/);if(!match)throw new Error(`Missing literal actionId at ${control.file}:${control.line}`);return{actionId:match[1],name:`Button at ${control.file}:${control.line}`,surface:coverage.surface,screen:control.file,ownerCore:"eventspace",status:"gated",permissions:[],preconditions:["executor-and-receipt-mapping-required"],inputSchema:"none",executor:"job",receiptSchema:"unconfigured",resultStates:["success","failed","pending","gated"],fallback:"show-gated",telemetry:"action-ledger/v1",lastReviewedAt:"2026-09-08"}});if(new Set(contracts.map(x=>x.actionId)).size!==contracts.length)throw new Error("Duplicate actionId");fs.writeFileSync("reports/action-registry.json",`${JSON.stringify({generatedAt:new Date().toISOString(),surface:coverage.surface,contracts},null,2)}\n`);console.log(`ACTION REGISTRY PASS: ${contracts.length} unique contracts; unconfigured executors remain gated.`);
