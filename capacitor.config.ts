import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = {
  appId: "live.phuclong.long",
  appName: "Long",
  webDir: "out",
  server: { androidScheme: "https" }
};
export default config;
