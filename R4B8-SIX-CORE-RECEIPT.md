# Long Mobi R4B8 — Action Contract + Six-Core Receipt

- 153/153 source buttons have unique Action IDs, handlers, `long-action/v3` contracts, input/output schemas and workflow links.
- Every click is routed through the fixed six-core receipt chain: EventSpace → Connection → Long AI → Long Scene → Flash Flow → Media.
- Each core emits a signed, hash-linked receipt; only a verified terminal API/provider/device/job receipt may create business success.
- Internal view/filter/navigation actions can complete locally with a signed executor receipt.
- OAuth, payment, render and physical-device actions remain gated until real external evidence is available.
- Home/Event structured selection, AI settings and outside channels, Store warehouse, Mixer console, integrated Chat and TV pairing flows are preserved.
- Audit: 153/153 coverage; no duplicate/static-in-map/dead button; six-core audit PASS; production build PASS.

Production requires `LONG_RECEIPT_SECRET` (32+ characters), `LONG_RUNTIME_MODE=production`, database migration and the credentials/adapters for each enabled external service.
